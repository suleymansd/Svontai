# FastAPI & Web
# Keep FastAPI/Starlette on the application-tested compatibility line. FastAPI
# 0.139 switched route internals and broke the frontend/backend contract gate.
fastapi==0.128.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
pydantic[email]>=2.5.3
pydantic-settings>=2.1.0

# Database (SQLite for dev)
sqlalchemy>=2.0.25
aiosqlite>=0.19.0
psycopg[binary]>=3.1.0
alembic>=1.13.0

# OpenAI
openai>=1.9.0

# HTTP Client
httpx>=0.26.0
redis>=5.0.0
sentry-sdk[fastapi]>=2.0.0

# Payments
stripe>=8.5.0

# Security & Encryption
cryptography>=42.0.0
pywebpush>=2.0.3

# Utils
python-dotenv>=1.0.0
